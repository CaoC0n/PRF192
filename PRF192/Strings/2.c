#include <stdio.h>
#include <string.h>


int main() {
	char name[50];
	scanf("%s", name);
	char address[50];
	scanf("%s", address);
	puts("OUTPUT:");
	printf("Name: %s\n", name);
	printf("Address: %s", address);
	return 0;
}
